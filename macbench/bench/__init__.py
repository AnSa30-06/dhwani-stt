"""macbench — measure the dhwani dictation engine on Apple hardware.

The engine was written and tuned on a Windows box that decodes 10-20x slower
than the machine it is scored on. That made the 30-point latency axis
unreadable, so three rounds of work went into the 70-point quality axis because
it was the only one visible. This package exists to read the other axis.
"""
