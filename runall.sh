#!/usr/bin/env bash
# You are in the right place. Just run:   bash runall.sh
# Everything it does, and every option, is explained in README-RUN-THIS.md
exec bash "$(dirname "$0")/macbench/runall.sh" "$@"
